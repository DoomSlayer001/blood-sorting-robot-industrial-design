# Legacy v1 Archive

This directory preserves `blood_sorting_robot_full_auto`, the previous automatically generated concept-model project.

The legacy project is retained for traceability and reference only. It is not the industrial-design mainline. Do not delete it, and do not treat its concept CAD or simulation outputs as production-ready industrial design files.

Large legacy STEP and GIF files are tracked by Git LFS according to the repository `.gitattributes` rules.
